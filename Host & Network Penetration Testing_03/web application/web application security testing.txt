security testing: 

vulnerability scanning
penetration testing
code review and static analysis.
authentication and authorization testing.
input validation and output encoding testing
session management testing
api security testing


web application threat and risk:


threat: potential danger even.
risk: probability of incident happening and potential impact of threat occurrence.


Web Application Architecture & Components:

client-server model.


Web Application Technologies:

client-side technologies:
HTML
CSS
JAVASCRIPT
Cookies and local storage


server-side technologies:
web server
application server
database server
server-side scripting language


communication and dataflow:
http


Data interchange:
exchange data btw different system and application.
compatible with the systems.(language, format etc)

Data interchange technologies:
API

example, quickbook.

Data interchange protocols:
json
XML
REST (GET, POST, PUT, DELETE)
SOAP


Security Technologies:
authentication and authorization mechanism.
encryption (ssl/tls)

external technologies:
CDNs
third-party libraries and framework


Introduction to HTTP:
http version:
HTTP 1.0
HTTP 1.1

types are:
http
https

http protocol basics:
request 
response

HTTP request:
request line: http method=URL=HTTP version
request header: user-agent=host=Accept=authorization=cookie
request body.
upgrade-insecure-requests=1 (if https supported then upgrade to it (1))
content-type= (type of data send in the body of request)
referer= (tell where this particular request from that request)


http request methods:
GET (considered safe)
POST (some what dangerous)
PUT (update more dangerous then post)
DELETE ()
PATCH (provide partial modification similar to PUT example changing permission of the file)

HEAD (similar to get but only retrieve the header header not body)

OPTIONS (communication options available by the resource)

TRACE (TRACE echoes back the exact HTTP request that the client sent to the server.)

etc.

HTTP request URL/PATH:
/= the home page of website is always



HTTP Response:
response line: status line =HTTP version=statuscode
response header: content-type=content-length=set-cookie=cache-control
response body:

response header:
HTTP version=statuscode
date (when the response was generated by server)
cache-control
server (tell the server used)
x-powered-by (what backend programming language used and also info about the OS)
DAV =(protocol that allow user to share/copy/move/edit file on web server)


 1 public
 2 private
 3 no-cache
 4 no-store
 5 max-age

content-type
content-encoding
server
content-length



status code:
200 ok
301 moved permanently
302 found (temporarily redirection)
400 bad request
401 unauthorized
403 forbidden
404 not found
500 internal server error


HTTP basic Lab:

browser                     web server
192.168.1.2                 192.168.1.3    example ip

1. client send Packet with Syn flag set.
2. server acknowledges the recipt of packet and        reponse with SYN ACK flag set to the client.
3. client acknowledges and send the packet with ACK flag set.


""""
SYN
SYN ACK
ACK
RST
FIN
""""


SYN=synchronization
ACK=acknowledges


wireshark:

tcp stream

export the (object)file that we get in encoded form when it is download it automatically decoded.


curl:

curl -v  target_ip

v=verbose

curl -I target_ip

I = head option

curl -v -X OPTIONS target_ip

-X options = tell what methods are allowed

curl -v -X PUT target_ip


Burpsuite:



dirb --url target_ip
# Dirb finds existing directories and files
             

curl target_ip/uploads  --upload-file  location-of-file


HTTPS:

ssl/tls



Website Crawling & Spidering:


Passive Crawling & Spidering with Burp Suite & OWASP ZAP

crawling= moving in the website (navigating around the web app)

spidering= automatically discovering new resources (URL).
it is loud.(active info gathering)

URLs =seeds
URL=seed

OWASP ZAP's Spider



















